<p style="color: green"><%= notice %></p>

<h1>Contas</h1>

<div id="contas">
  <% @contas.each do |conta| %>
    <%= render conta %>
    <p>
      <%= link_to "Show this contas", conta %>
    </p>
  <% end %>
</div>

<%= link_to "New contas", new_conta_path %>
