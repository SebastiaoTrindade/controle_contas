<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Dashboard Responsivo" %></title>
    <!-- Bootstrap CSS -->
    <%= stylesheet_link_tag 'https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css', media: 'all' %>
    <!-- Icons -->
    <%= stylesheet_link_tag 'https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css', media: 'all' %>
    <!-- Custom CSS -->
    <%= stylesheet_link_tag 'dashboard', media: 'all' %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
</head>
<body>
    <div id="dashboard-wrapper">       
        <!-- Navbar -->
         <%= render 'shared/navbar' %>                 

        <!-- Main Content -->
        <div id="main-content" class="p-4">
            <div class="container mt-4">  
                <%= render 'shared/cards' %>              
                <%= yield %>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <%= javascript_include_tag 'https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js', 'data-turbo-track': 'reload' %>
</body>
</html>
